import { Meta, ArgTypes, Canvas } from "@storybook/addon-docs/blocks";
import { TreeDataSwitch } from "@digital-science/figshare-fcl/latest/TreeDataSwitch";
import * as TreeDataSwitchStories from "./treedataswitch.stories.jsx";

<Meta of={TreeDataSwitchStories} />

# TreeDataSwitch

---

- [Overview](#overview)
- [Props](#props-table)
- [Examples](#examples)

## Overview

A hierarchical tree component with checkbox switches for toggling item states. Supports searching, expandable/collapsible nodes, read-only mode, and custom action rendering.

The component can be imported from `@digital-science/figshare-fcl/latest`.

## Props Table

<ArgTypes of={TreeDataSwitch} />

## Examples

### Basic TreeDataSwitch

<Canvas of={TreeDataSwitchStories.BasicTreeDataSwitch} />

```jsx
import { TreeDataSwitch } from "@digital-science/figshare-fcl/latest/TreeDataSwitch";
import { useState } from "react";

export default function TreeExample() {
  const treeData = {
    children: ["1", "2"],
    nodes: {
      "1": {
        id: "1",
        level: 0,
        path: ["1"],
        datum: { name: "Parent Item 1" },
        state: { expanded: true, checked: false, disabled: false },
        children: ["1-1", "1-2"],
        nodes: {
          "1-1": {
            id: "1-1",
            level: 1,
            path: ["1", "1-1"],
            datum: { name: "Child Item 1-1" },
            state: { expanded: false, checked: false, disabled: false },
            children: [],
            nodes: {},
            metadata: {}
          },
          "1-2": {
            id: "1-2",
            level: 1,
            path: ["1", "1-2"],
            datum: { name: "Child Item 1-2" },
            state: { expanded: false, checked: false, disabled: false },
            children: [],
            nodes: {},
            metadata: {}
          }
        },
        metadata: {}
      },
      "2": {
        id: "2",
        level: 0,
        path: ["2"],
        datum: { name: "Parent Item 2" },
        state: { expanded: false, checked: false, disabled: false },
        children: [],
        nodes: {},
        metadata: {}
      }
    }
  };

  const [tree, setTree] = useState(treeData);

  const handleChange = ({ tree: newTree }) => {
    setTree({ ...newTree });
  };

  return (
    <TreeDataSwitch
      name="items"
      tree={tree}
      onChange={handleChange}
      searchPlaceholder="Search items..."
      noSearchResultsMessage="No matching items found"
    />
  );
}
```

### TreeDataSwitch Features

<Canvas of={TreeDataSwitchStories.TreeDataSwitchFeatures} />

```jsx
import { TreeDataSwitch } from "@digital-science/figshare-fcl/latest/TreeDataSwitch";

// Tree structure with metadata
const treeData = {
  children: ["1"],
  nodes: {
    "1": {
      id: "1",
      level: 0,
      path: ["1"],
      datum: { name: "Item with Tooltip" },
      state: { expanded: true, checked: false, disabled: false },
      children: [],
      nodes: {},
      metadata: {
        tooltip: "This item has a helpful tooltip"
      }
    }
  }
};

// Render with custom actions
<TreeDataSwitch
  name="items"
  tree={treeData}
  onChange={handleChange}
  renderActions={({ node, checked, readOnly }) => (
    <button>Custom Action</button>
  )}
  renderBulkActions={({ tree, onUpdate }) => (
    <button>Bulk Select All</button>
  )}
/>
```

### Read-only Mode

<Canvas of={TreeDataSwitchStories.ReadOnlyMode} />

```jsx
import { TreeDataSwitch } from "@digital-science/figshare-fcl/latest/TreeDataSwitch";

<TreeDataSwitch
  name="items"
  tree={treeData}
  onChange={handleChange}
  readOnly={true}
/>
```
