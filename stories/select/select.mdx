import { Meta, ArgTypes, Canvas } from '@storybook/addon-docs/blocks';
import { Select } from "../../packages/ui/select";
import * as SelectStories from "./select.stories.jsx";

<Meta of={SelectStories} />

# Select
---

- [Overview](#overview)
- [Props](#props)
- [Examples](#examples)

## Overview
Basic Select component.


<Canvas of={SelectStories.Overview} />


The component can be imported from `@digital-science/figshare-fcl/Select`.

```jsx
import React, { Component } from "react";
import PropTypes from "prop-types";

import { Select, Option } from "@digital-science/figshare-fcl/Select";

const SelectExample = () => {
  return(
    <div style={{ display: "flex", flexDirection: "column", gap: "15px" }}>
    <Select loading={true}>
      <Option value={1}>One</Option>
      <Option value={2}>Two</Option>
    </Select>
    </div>
  );
 }
```

## Props

<ArgTypes of={Select} />

## Examples

<Canvas of={SelectStories.Examples} />
