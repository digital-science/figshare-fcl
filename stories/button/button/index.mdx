import { Button } from "@digital-science/figshare-fcl/button";
import * as ButtonStories from "./index.stories.jsx";

import { Meta, ArgTypes, Canvas } from "@storybook/addon-docs/blocks";

<Meta of={ButtonStories} />

# Button
---

- [Overview](#overview)
- [Props](#props-table)
- [Examples](#examples)

## Overview
Basic button component. Allows users to trigger actions by interacting with the button by mouse or keyboard.
Comes with different variations depending on the type of action we want to emphasise to the user.


<Canvas of={ButtonStories.Overview} />


Can be imported from:

```jsx
import { Button } from "@digital-science/figshare-fcl/button";

<Button 
  theme="primary" 
  onClick={() => {}} 
  tooltip="This button has a tooltip"
>
  Main action
</Button>
<Button 
  theme="secondary" 
  onClick={() => {}} 
  tooltip="This button has a tooltip"
>
  Secondary action
</Button>
```

## Props Table
<ArgTypes of={Button} />
*Accepts additional properties that can be supplied to a BaseButton component.*

## Examples

<Canvas of={ButtonStories.AllThemesAndSizes} />

<Canvas of={ButtonStories.InvertedTheme} />
