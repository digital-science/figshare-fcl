import TextInput from "@digital-science/figshare-fcl/input/text";
import * as TextStories from "./text.stories.jsx";

import { Meta, ArgTypes, Canvas } from "@storybook/addon-docs/blocks";

<Meta of={TextStories} />

# Text input
---
- [Overview](#overview)
- [Props](#props-table)
- [Examples](#examples)

## Overview

A text input component. Allows users to type in and store text values.


<Canvas of={TextStories.Overview} />


Can be imported directly or referenced as the default `Input` component variation:

```jsx
import React from "react";
import Input from "@digital-science/figshare-fcl/input";
import TextInput from "@digital-science/figshare-fcl/input/text";

function TextInputs() {
  return (
    <TextInput onChange={(event) => {}} />
    <Input onChange={(event) => {}} />
    <Input type="text" onChange={(event) => {}} />
  );
}
```

## Props Table
<ArgTypes of={TextInput} />

## Examples
<Canvas of={TextStories.Examples} />
