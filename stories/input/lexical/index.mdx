import Editor from "@digital-science/figshare-fcl/textEditor/Lexical";
import * as TextEditorStories from "./lexical.stories.jsx";

import { Meta, ArgTypes, Canvas } from "@storybook/addon-docs/blocks";

<Meta of={TextEditorStories} />

# Text editor
---
- [Overview](#overview)
- [Props](#props-table)
- [Examples](#examples)

## Overview

A textarea which allows users to input more complex HTML markup text. 
Users can mark portions or the whole content as paragraphs, headings or emphasise or underline text in various ways.


<Canvas of={TextEditorStories.Overview} />


The component can be imported from:

```jsx
import Editor from "@digital-science/figshare-fcl/textEditor";

class MyTextEditor extends React.Component {
  render() {
    return (
      const [text, setText] = useState("<b>Initial text or markup</b>");
      return <Editor onChange={(e) => {
        setText(e.target.value);
      }} value={text} />;
    );
  }
}
```

## Props Table

<ArgTypes of={Editor} />

## Examples

<Canvas of={TextEditorStories.Examples} />
