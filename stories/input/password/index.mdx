import PasswordInput from "@digital-science/figshare-fcl/input/password";
import * as PasswordStories from "./password.stories.jsx";

import { Meta, ArgTypes, Canvas } from "@storybook/addon-docs/blocks";

<Meta of={PasswordStories} />

# Password input
---
- [Overview](#overview)
- [Props](#props-table)
- [Examples](#examples)

## Overview
Password input component. Allows users to safely input passwords, and to temporarily toggle visibility.


<Canvas of={PasswordStories.Overview} />


Can be imported directly or referenced as an `Input` component variation, by setting `type="password"`:

```jsx
import React from "react";
import Input from "@digital-science/figshare-fcl/input";
import PasswordInput from "@digital-science/figshare-fcl/input/password";

function PasswordInputs() {
  return (
    <PasswordInput onChange={(event) => {}} />
    <Input type="password" onChange={(event) => {}} />
  );
}
```

## Props Table
<ArgTypes of={PasswordInput} />

## Examples

<Canvas of={PasswordStories.Examples} />
